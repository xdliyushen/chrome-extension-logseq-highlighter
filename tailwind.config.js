module.exports = {
    content: ['./public/*.html'],
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [
        require('tailwind-scrollbar-hide'),
    ],
}
