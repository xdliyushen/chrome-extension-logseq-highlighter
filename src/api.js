function addHightlight(normalizedSelection) {
    console.log(normalizedSelection);
}

function removeHighlight(normalizedSelection) {

}

const API = {
    addHightlight, removeHighlight 
}

export default API;